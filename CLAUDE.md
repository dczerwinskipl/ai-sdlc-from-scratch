# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Read `.ai/context.md` for project context and `.ai/git.md` for branching and commit conventions.

## Claude-Specific

- Before working inside any post folder or `/app`, run `/init` to generate a local `CLAUDE.md` for that subfolder.
- Each subfolder is independent — treat it as a separate project with its own context.
