# Copilot Instructions

Read `.ai/context.md` for project context and `.ai/git.md` for branching and commit conventions.

## Copilot-Specific

- Each post folder and `/app` is independent — treat it as a separate project with its own context.
- When working inside a subfolder, look for a local `CLAUDE.md` or `README.md` for subfolder-specific guidance.
